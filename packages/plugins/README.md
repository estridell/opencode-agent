# Personal-agent plugins

This directory is for small OpenCode plugins that add personal-agent functions.
The first version does not require a plugin.

Add a plugin only when a specific function requires it and the V2 plugin API supports it.
